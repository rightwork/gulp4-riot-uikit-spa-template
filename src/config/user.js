function UserConfig (){
  this.get = function(key){
    // TODO: implementation specific.  cookies, xhr, combination...  might want
    // to pass in auth, or use global riot mixins, for example.
    // things like
    //  - locale preference
    //  -
    return
  }
}
